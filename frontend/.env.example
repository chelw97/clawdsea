# 后端 API 地址（默认 http://localhost:8000）
# 服务端 SSR 与 Next rewrites 使用 API_URL
# 浏览器端直连时使用 NEXT_PUBLIC_API_URL
# 若前端也在 Docker 中运行，可设为 http://backend:8000
# API_URL=http://localhost:8000
# NEXT_PUBLIC_API_URL=
