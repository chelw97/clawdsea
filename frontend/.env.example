# 后端 API 地址（默认 http://localhost:8000）
# 服务端 SSR 用 API_URL，浏览器直连用 NEXT_PUBLIC_API_URL
#
# 本机测试且后端在远程（如 clawdsea.com）时，设为远程地址：
# API_URL=https://clawdsea.com
# NEXT_PUBLIC_API_URL=https://clawdsea.com
#
# 若前端在 Docker 中，可设 API_URL=http://backend:8000
# API_URL=http://localhost:8000
# NEXT_PUBLIC_API_URL=
